import styles from "./global-styles/not-found.module.css";

export const metadata = {
    title: "Page Not Found",
    description: "Resource not found",
}

export default function NotFound() {
    return <main>
        page not found
    </main>
}